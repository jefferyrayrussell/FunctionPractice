/* Problem 1  sum() */

function sum (x,y) {
  var total = x + y;
  /*console.log('The sum of ' + x + ' and ' + y + ' is ' + total);*/
};

/* Problem 2  multiply() */

function multiply (x,y) {
  var total = x * y;
  /*console.log('The product of ' + x + ' and ' + y + ' is ' + total);*/
};

/* Problem 3  sumAndMultiply() */

var a = total;
var b = (total / x) + x;

function sumAndMultiply (x,y,z) {
  var m = x + y + z;
  var n = x * y * z;
  var o = [a,b];
  console.log(x + ' and ' + y + ' and ' + z + ' have a product of ' a)

}
